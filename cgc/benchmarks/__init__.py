# cgc.benchmarks — Automated Benchmark Testing & Reproducibility Verification
#
# Usage:
#   python -m cgc.benchmarks.verify          # verify against reference
#   python -m cgc.benchmarks.generate_reference  # regenerate reference (dev only)
